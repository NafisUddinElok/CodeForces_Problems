#include <stdio.h>
#include <stdlib.h>
int main()
{
   int t;
   scanf("%d", &t);
   while(t--)
   {
     int l, r, d, u;
     scanf("%d %d %d %d", &l, &r, &d, &u);
     if( l == r && r == d && d == u) printf("YES\n");
     else printf("NO\n");
   }
}
